# Core
numpy>=1.26
pandas>=2.1
pyarrow>=14.0
scipy>=1.11
scikit-learn>=1.4
pyyaml>=6.0

# Data access
wrds>=3.2          # WRDS (TAQ, CRSP); requires WRDS account
yfinance>=0.2      # OHLC breadth universe
fredapi>=0.5       # FRED state variables
requests>=2.31

# Forecasters
lightgbm>=4.3
statsmodels>=0.14
arch>=6.3          # GARCH-t / volatility baselines
# Heavyweight forecaster deps (installed in Phase 2 when first needed):
# torch>=2.2         # small GRU forecaster
# chronos-forecasting>=1.2
# timesfm

# Regimes
hmmlearn>=0.3

# Tracking & evaluation
mlflow>=2.10
matplotlib>=3.8

# Dev
pytest>=8.0
